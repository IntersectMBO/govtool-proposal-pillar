'use strict';

/**
 * budget-discussion-currency-list service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::budget-discussion-currency-list.budget-discussion-currency-list');
